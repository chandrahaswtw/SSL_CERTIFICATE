<table class="table table-hover">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">APP ID</th>
                <th scope="col">APP NAME</th>
                <th scope="col">SERVER NAME</th>
                <th scope="col">EXPIRY DATE</th>
                <th></th>
                <th></th>
            </tr>
            </thead>
    
            {{#each ALL_RECORDS}}
            <tr>
                <td scope="col" class="counterCell"></td>
                <td scope="col">{{this.doc.appId}}</td>
                <td scope="col">{{this.doc.appName}}</td>
                <td scope="col">{{this.doc.serverName}}</td>
                <td scope="col">{{this.doc.expDate}}</td>
                <td scope="col" id="{{this.doc._id}}"><i class="fas fa-eye"></i></td>
                <td scope="col" id="{{this.doc._id}}"><i class="fas fa-edit"></i></td>
            </tr>
            {{/each}}
        </table>
    </script>
